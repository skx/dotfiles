
" use shell with ctrl-z
map <C-Z> :shell<CR>

" Quit with 'q' instead of ':q'. VERY useful!
map q :q<CR>

" we use the "real" keys, not the arrows.
noremap <Up> <nop>
noremap <Down> <nop>
noremap <Left> <nop>
noremap <Right> <nop>
